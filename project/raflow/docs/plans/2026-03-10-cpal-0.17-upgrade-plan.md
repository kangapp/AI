# CPAL 0.17 升级实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标:** 升级 CPAL 到 0.17 版本，简化代码架构，解决 macOS open 启动时的音频权限问题

**架构:** 统一使用 CPAL 0.17，移除冗余的 CoreAudio 绕过实现。CPAL 0.17 已修复 open 启动时触发麦克风权限的问题。

**技术栈:** Rust, CPAL 0.17, Tauri 2.0

---

## 任务 1: 升级 CPAL 依赖版本

**文件:**
- 修改: `src-tauri/Cargo.toml:27`

**步骤 1: 修改 Cargo.toml 中的 cpal 版本**

```toml
# 将
cpal = "0.15"
# 改为
cpal = "0.17"
```

**步骤 2: 验证依赖可以解析**

Run: `cd src-tauri && cargo update cpal && cargo check 2>&1 | head -50`

预期: 无版本冲突错误

**步骤 3: 提交**

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -m "chore(deps): upgrade cpal from 0.15 to 0.17"
```

---

## 任务 2: 检查 CPAL 0.17 API 兼容性

**文件:**
- 查阅: `src-tauri/src/audio/capture.rs`

**步骤 1: 检查 API 变化**

Run: `cd src-tauri && cargo check 2>&1 | grep -E "error|warning.*cpal" | head -20`

预期: 显示 API 不兼容的具体错误（如有）

**步骤 2: 记录需要修改的 API 调用**

根据 CPAL 0.17 changelog，主要变化:
- `SampleFormat` 可能需要调整
- `default_input_config()` 返回类型可能变化
- `StreamConfig` 创建方式可能变化

预期: 记录需要修改的代码位置

**步骤 3: 提交**

```bash
git commit -m "chore: check cpal 0.17 api compatibility"
```

---

## 任务 3: 更新 capture.rs 使用 CPAL 0.17 API

**文件:**
- 修改: `src-tauri/src/audio/capture.rs`

**步骤 1: 修复 API 不兼容问题**

根据 cargo check 错误，修复以下可能的 API 问题:

```rust
// 可能需要修改的部分:

// 1. SampleFormat 处理可能变化
let sample_format = config.sample_format();

// 2. StreamConfig 创建
let stream_config: StreamConfig = config.clone().into();

// 3. 检查 default_input_config 返回类型
```

**步骤 2: 验证代码可以编译**

Run: `cd src-tauri && cargo check 2>&1 | tail -10`

预期: 编译成功，无错误

**步骤 3: 提交**

```bash
git add src-tauri/src/audio/capture.rs
git commit -m "fix(audio): adapt to cpal 0.17 api"
```

---

## 任务 4: 简化 audio 模块 - 移除条件编译

**文件:**
- 修改: `src-tauri/src/audio/mod.rs`

**步骤 1: 简化 mod.rs - 统一使用 capture.rs**

```rust
// 将当前代码:
// #[cfg(target_os = "macos")]
// mod capture_avaudio;
// #[cfg(not(target_os = "macos"))]
// mod capture;
//
// #[cfg(target_os = "macos")]
// pub use capture_avaudio::*;
// #[cfg(not(target_os = "macos"))]
// pub use capture::*;

// 改为:
mod capture;
mod pipeline;
mod resampler;

pub use capture::*;
pub use pipeline::*;
pub use resampler::*;
```

**步骤 2: 验证编译**

Run: `cd src-tauri && cargo check 2>&1 | tail -10`

预期: 编译成功

**步骤 3: 提交**

```bash
git add src-tauri/src/audio/mod.rs
git commit -m "refactor(audio): simplify module structure, use cpal for all platforms"
```

---

## 任务 5: 更新 pipeline.rs - 移除 wake_up_audio_device 调用

**文件:**
- 修改: `src-tauri/src/audio/pipeline.rs`

**步骤 1: 移除 wake_up_audio_device 调用**

找到并删除:
```rust
// 删除这一行:
// wake_up_audio_device();
```

在 `pipeline.rs:151` 附近，删除:
```rust
// Wake up audio device before starting capture
// This is a workaround for macOS open launch issue
wake_up_audio_device();
```

**步骤 2: 验证编译**

Run: `cd src-tauri && cargo check 2>&1 | tail -10`

预期: 编译成功

**步骤 3: 提交**

```bash
git add src-tauri/src/audio/pipeline.rs
git commit -m "refactor(audio): remove wake_up_audio_device workaround"
```

---

## 任务 6: 删除冗余的音频捕获实现

**文件:**
- 删除: `src-tauri/src/audio/capture_avaudio.rs`
- 删除: `src-tauri/src/audio/capture_avfoundation.rs`

**步骤 1: 删除文件**

```bash
rm src-tauri/src/audio/capture_avaudio.rs
rm src-tauri/src/audio/capture_avfoundation.rs
```

**步骤 2: 验证编译**

Run: `cd src-tauri && cargo check 2>&1 | tail -10`

预期: 编译成功

**步骤 3: 提交**

```bash
git add -A
git commit -m "refactor(audio): remove redundant capture implementations"
```

---

## 任务 7: 测试 open 启动场景

**文件:**
- 构建: `src-tauri/target/release/bundle/macos/RaFlow.app`
- 测试: 使用 `open RaFlow.app` 启动应用

**步骤 1: 构建 release 版本**

Run: `cd src-tauri && cargo build --release 2>&1 | tail -20`

预期: 构建成功

**步骤 2: 打包应用**

Run: `cd src-tauri && cargo tauri build 2>&1 | tail -30`

预期: 打包成功，生成 RaFlow.app

**步骤 3: 测试 open 启动**

```bash
# 找到打包后的 app
APP_PATH=$(find src-tauri/target/release/bundle/macos -name "*.app" -type d | head -1)
echo "Testing app at: $APP_PATH"

# 使用 open 启动
open "$APP_PATH"

# 检查应用是否正常运行
# 观察日志输出，确认音频初始化成功
```

预期: 应用正常启动，无麦克风权限错误

**步骤 4: 提交**

```bash
git commit -m "test(audio): verify open launch works with cpal 0.17"
```

---

## 执行选项

**Plan complete and saved to `docs/plans/2026-03-10-cpal-0.17-upgrade-plan.md`. Two execution options:**

**1. Subagent-Driven (this session)** - I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Parallel Session (separate)** - Open new session with executing-plans, batch execution with checkpoints

**Which approach?**
